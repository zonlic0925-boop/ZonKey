# ZonKey 运行时依赖（Python 3.11）
# PDF 引擎（Phase M：100% 宽松许可，零 AGPL——渲染 pypdfium2 / 抽取 pdfplumber / 写入 pikepdf+reportlab）
numpy>=2.0,<3
opencv-python>=4.10
rapidocr-onnxruntime==1.4.4
onnxruntime>=1.19
Pillow>=10.0

# Web 桌面壳 + API
fastapi>=0.115
uvicorn[standard]>=0.32
pywebview>=5.0
python-multipart>=0.0.9
python-docx>=1.1

# P2 转换引擎（100% 宽松许可选型，见 docs/ILOVEPDF_INTEGRATION_PLAN.md §3）
pypdfium2>=5.0
pikepdf>=9.0
pdfplumber>=0.11
python-pptx>=1.0
openpyxl>=3.1
mammoth>=1.6
reportlab>=4.0

# P3 PDF 证书签名（PAdES，MIT 许可；用户自带 PEM 证书+私钥）
pyhanko>=0.36
pyhanko-certvalidator>=0.31

# Windows Office COM 兜底（PPT/Word/Excel 转 PDF 的能力探测后路）
pywin32>=306; sys_platform == "win32"

# 语音转写（文字工坊，纯离线；模型首次使用时自动下载）
faster-whisper>=1.0

# 开发与回归
pytest>=8.0
psutil>=5.9

# P3 证书级 PDF 签名
pyHanko>=0.21
